# TimeHUD – Python dependencies
# Install with: pip install -r requirements.txt

# Required
PyQt6>=6.11.0

# Optional – enables global hotkeys (Ctrl+Shift+Space etc.)
# Comment out if you don't need them or have install issues.
pynput>=1.8.1
